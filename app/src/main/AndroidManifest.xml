<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pack2school">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sign_up_parent_page"></activity>
        <activity android:name=".sign_up_student_page" />
        <activity android:name=".sign_up_teacher_page" />
        <activity android:name=".parent_main_page" />
        <activity android:name=".student_main_page" />
        <activity android:name=".teacher_main_page" />
        <activity android:name=".sign_in_main_page" />
        <activity android:name=".sign_up_main_page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>